
#include "../philosophers.h"

int	ft_return(int mark)
{
	if (mark == 0)
	{
		printf("Arg requireds : [number_of_philosophers] [time_to_die] [time_to_eat]
		[time_to_sleep] (opt.)[number_of_time_each_philosopher_must_eat]\n");
		return (0);
	}
}
